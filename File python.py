f = open('text.txt', 'r')
print(f.read(23))

                # opening files
#If we type 'r' - open file for reading
#'w' - open file for writing, when we open information will be delete, if we don't have
#have file , it's created.
#if we type 'x' open file for writing, if file does not exist we have (error)
#'a' open for file and we (add) information at the end of the file.
#'b' oben in binary format
#'t' open file in (text) format (default)
#'+' open for reading and writing.
